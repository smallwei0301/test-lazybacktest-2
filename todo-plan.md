# 工作待辦

1. 處理 `public/app/js/backtest.js` 與 `public/app/js/batch-optimization.js` 中 eslint `no-unused-vars` 警告。（進行中，已修正部分 catch 區塊與未使用常數）
2. 調整 `showBatchProgress` 重設時的狀態欄位，確保時間預估正常。（已完成）
3. 檢視 `next lint` 設定並移除已棄用選項，恢復 `npm run lint`。
