{
  "name": "test-lazybacktest",
  "version": "0.1.0",
  "private": true,
  "description": "LazyBacktest with Netlify Functions proxy",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "plugin:parity": "node scripts/strategy-plugin-parity-check.js"
  },
  "dependencies": {
    "@netlify/blobs": "^6.0.0",
    "node-fetch": "^2.6.7"
  }
}
